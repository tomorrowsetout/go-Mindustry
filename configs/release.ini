; mdt-server 启动释放控制
;
; 本文件只控制“是否释放内置地图资源(assets/worlds)到磁盘”。
; configs/ 目录只存放 INI 配置文件；assets/data/mods/logs 等运行目录都在 EXE 根目录。
;
; released:
;   0 = 下次启动会执行释放（会覆盖写入 assets/worlds 下同名文件）
;   1 = 标记为已释放（正常情况下程序释放完会自动写 1，二次启动不会释放）
;
[release]
released = 1
