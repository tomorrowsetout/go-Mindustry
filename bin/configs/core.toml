# 核心配置

[core]
dual_core_enabled = true
tps = 120

[runtime]
# 0 = 自动使用本机逻辑核数 (runtime.NumCPU)
cores = 0
scheduler_enabled = true

[memory]
check_interval_sec = 5
free_os_memory = false
gc_trigger_mb = 0
limit_mb = 0
startup_max_mb = 0
